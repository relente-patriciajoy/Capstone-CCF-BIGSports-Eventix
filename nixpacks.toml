[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo_mysql", "php82Extensions.gd", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.openssl", "mariadb"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT -t eventsys/codes/php"